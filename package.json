{
  "name": "hikvision-doorbell-card",
  "version": "0.1.7",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "lit": "^3.2.0"
  }
}
